<% if resource.persisted? %>
  window.location = '<%= resource.active_for_authentication? ? after_sign_up_path_for(resource) : after_inactive_sign_up_path_for(resource) %>';
<% else %>
  $('#modal-sign-up').html('<%= escape_javascript render("shared/form_sign_up") %>')
<% end %>